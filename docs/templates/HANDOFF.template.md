# HANDOFF — {game}

> 兩站接力（實機端 ↔ 翻譯端）與跨對話交接共用的備忘。每次 `agt handoff pack` 前，在「交接紀錄」最上方新增一段。
> 專案完結、教訓回寫 NOTES／docs 後刪除本檔（不進 git）。

## 專案摘要

- 引擎／版本／變體：
- 編碼：
- 字串數與 context 分布：
- 字型與缺字對策（覆蓋率數字）：
- 絕不導出的抽樣結論：

## 交接紀錄

### #{seq} {from_site} → {to_site}  {date}

- 本站完成：
- 請對方做：（翻譯端 smoke 樣本用的 `--filter` 正則與 `--limit` 寫在這裡）
- 需要對方回答：
- 實機回報：（開得起來／崩潰＋dmp 路徑／□ 缺字／亂碼／選單正常…）
- 已知問題／暫不處理：

## 變體紀錄（A/B）

| 變體 | 只差這一件事 | 實機結果 |
|---|---|---|

## 跨對話備忘

（context 不夠、要換新對話時寫：做到哪、下一步、卡在哪）
